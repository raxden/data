name: Continuous Integration (PR)

on:
  pull_request:
    types: [opened, synchronize, reopened, ready_for_review]

  workflow_dispatch:

concurrency:
  # Ensure only one run per PR is active
  group: build-pr-${{ github.event.pull_request.number }}
  cancel-in-progress: true

permissions:
  contents: read
  actions: write

env:
  # Increment to invalidate Gradle cache when needed
  CACHE_VERSION: 1
  # Increase Gradle heap size to prevent OOM errors in large builds
  GRADLE_OPTS: >
    -Dorg.gradle.jvmargs=-Xmx4g
    -Dkotlin.daemon.jvm.options=-Xmx2g
  # Enable debug for Gradle cache action
  GRADLE_BUILD_ACTION_CACHE_DEBUG_ENABLED: true
  # Artifact retention
  RETENTION_DAYS: 1
  # Artifact type (APK for Firebase App Distribution, AAB requires Google Play linking)
  ARTIFACT_TYPE: "APK"
  # Build configuration
  BUILD_TYPE: "Debug"

jobs:

  info:
    name: Workflow Information
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - uses: actions/checkout@v6
        with: { fetch-depth: 0 }

      - name: Print workflow information
        uses: ./.github/actions/info
